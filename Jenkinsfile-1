pipeline {
    agent any
    stages {
        stage('Example Username/Password') {
            environment {
                MY_FILES = sh(script: 'ls -l', returnStdout: true)
            }
            steps {
                sh 'echo "Service user is $MY_FILES"'
            }
        }
    }
}
